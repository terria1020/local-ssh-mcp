# Local SSH MCP Server - Environment Variables
# Copy this file to .env and customize values as needed

# ==================================================
# Server Configuration
# ==================================================

# Server port (default: 4000)
PORT=4000

# Node environment (development | production)
NODE_ENV=development

# ==================================================
# Logging Configuration
# ==================================================

# Log level (error | warn | info | debug)
# Default: info
LOG_LEVEL=info

# ==================================================
# SSH Session Configuration
# ==================================================

# Session timeout in milliseconds (default: 300000 = 5 minutes)
# Used for persistent SSH sessions
SESSION_TIMEOUT=300000

# ==================================================
# Credentials Configuration (Optional)
# ==================================================

# Path to credentials.json file (optional)
# If not set, defaults to ./credentials.json in project root
# CREDENTIALS_FILE=/path/to/credentials.json

# ==================================================
# Legacy SSH Authentication (Optional, Not Recommended)
# ==================================================

# Note: These variables are optional and provided for backward compatibility.
# New installations should use credentials.json instead.

# Path to SSH private key file (optional)
# Example: /Users/yourusername/.ssh/id_rsa
# SSH_KEY_PATH=/path/to/ssh/key

# SSH key passphrase (optional, base64 encoded)
# Only needed if your SSH private key is password-protected
# SSH_PASSPHRASE=
