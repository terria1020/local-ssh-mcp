# Server Configuration
PORT=4000
NODE_ENV=development

# SSH Configuration
# You can use either key-based authentication OR password-based authentication

# Option 1: Key-based authentication (recommended)
# Path to your SSH private key file
SSH_KEY_PATH=/Users/yourusername/.ssh/id_rsa

# SSH key passphrase (optional)
# Leave empty or omit if your SSH key has no passphrase
SSH_PASSPHRASE=

# Option 2: Password-based authentication
# NOTE: Password can also be provided per-request via API
# Leave SSH_KEY_PATH empty if you want to use password-only authentication
# WARNING: Passwords are transmitted in plain text over localhost

# API Security
# Passphrase for JWT token issuance (used at /auth endpoint)
# This is NOT stored in zshrc - only the issued JWT token is stored
TOKEN_PASSPHRASE=my-secure-passphrase-change-this

# JWT Configuration
# Secret key for signing JWT tokens
# Generate a secure random key: openssl rand -hex 64
JWT_SECRET_KEY=your-jwt-secret-key-here-use-openssl-rand-hex-64

# JWT Issuer identifier (must match in JWT payload)
JWT_ISSUER=local-ssh-mcp-server

# MCP Server URL (for helper scripts)
# This is used by scripts/ssh-mcp-run.sh
MCP_SERVER_URL=http://127.0.0.1:4000

# Logging
# Options: error, warn, info, debug
LOG_LEVEL=info
